import React, { useState } from "react";

type CountergenResultsProps = {};

const CountergenResults = (props: CountergenResultsProps) => {
  return <p>CountergenResults</p>;
};

export default CountergenResults;
